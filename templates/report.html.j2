<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>{{ site_title }} — {{ date }}</title>
<link rel="stylesheet" href="https://unpkg.com/mvp.css">
<style>
.grid { display: grid; grid-template-columns: repeat(auto-fill,minmax(280px,1fr)); gap: 14px; }
.card { border: 1px solid #eee; border-radius: 12px; padding: 12px; background: #fff; }
.app { display: grid; grid-template-columns: 64px 1fr; gap: 10px; align-items: center; }
.app img { width: 64px; height: 64px; border-radius: 12px; }
.mono { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono","Courier New",monospace; font-size: 0.85rem; color:#555; }
.tag { padding: 2px 6px; border-radius: 10px; background: #eee; font-size: 0.75rem; }
small .tag { margin-left: 6px; }
h3 small { font-weight: normal; }
hr { border: none; border-top: 1px solid #eee; margin: 8px 0 16px; }
</style>
</head>
<body>
<header>
  <h1>{{ site_title }}</h1>
  <p>{{ country|upper }} — {{ date }} — <strong>{{ total_new }}</strong> first-ever entrants.</p>
  <p><a href="{{ base_url }}/">← Back to Index</a></p>
</header>
<main>
  {% for section in sections %}
    <article class="card">
      <h3>{{ section.feed_label }} <small class="tag">{{ section.genre_label }}</small></h3>
      <p class="mono">{{ section.count }} new apps</p>
      <div class="grid">
        {% for app in section.apps %}
          <div class="app">
            <img src="{{ app.artwork }}" alt="">
            <div>
              <div><a href="{{ app.url }}">{{ app.name }}</a></div>
              <small>{{ app.artist }}</small><br/>
              <small class="mono">id: {{ app.id }}{% if app.rank is not none %} — rank {{ app.rank }}{% endif %}</small>
            </div>
          </div>
        {% endfor %}
      </div>
    </article>
  {% endfor %}
</main>
<footer><p>Generated {{ now }}</p></footer>
</body>
</html>
