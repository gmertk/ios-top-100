<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>{{ site_title }}</title>
<link rel="stylesheet" href="https://unpkg.com/mvp.css">
<style>
  .tag { padding: 2px 6px; border-radius: 10px; background: #eee; font-size: 0.85rem; }
</style>
</head>
<body>
<header>
  <h1>{{ site_title }}</h1>
  <p>First-ever entrants into iOS Top-100 ({{ country|upper }}). Updated daily.</p>
</header>
<main>
  <h2>Reports</h2>
  <ul>
    {% for r in reports %}
      <li>
        <a href="{{ r.href }}">{{ r.date }}</a>
        <span class="tag">{{ r.total_new }} new apps</span>
      </li>
    {% endfor %}
  </ul>
</main>
<footer><p>Generated {{ now }}</p></footer>
</body>
</html>
